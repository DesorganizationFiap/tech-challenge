apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce # pode ser montado como leitura-gravação por um único nó
  resources:
    requests:
      storage: 1Gi
