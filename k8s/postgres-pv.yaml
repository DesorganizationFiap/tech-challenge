apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi # pode ser montado como leitura-gravação por um único nó
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
